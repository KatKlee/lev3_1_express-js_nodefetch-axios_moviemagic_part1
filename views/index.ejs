<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Moviemagic</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <header>
        <h2>MOVIEMAGIC</h2>
        <div>
            <input type="text" name="" id="" placeholder="search film …">
            <button>Search</button>
        </div>
    </header>
    <main class="mainIndex">
        <p>Hier steht ein Platzhalter für alle verfügabren Genres</p>
        <h1>Popular Movies</h1>
        <section class="sectionIndex">
            <% movies.results.forEach(item=>{%>
                <a href="/movie<%=item.id%>">
                    <img src="https://image.tmdb.org/t/p/original<%= item.poster_path%>" alt="<%=item.title%>">
                    <p>
                        <%=item.title%>
                    </p>
                </a>
                <%})%>
        </section>
    </main>
    <footer>
        <p>
            Seite <%= movies.page %> von <%=movies.total_pages%>
        </p>
        <% for (let i=movies.page; i < movies.morepages; i++) { if(movies.morepages<500) {%>
            <a href="/<%= i %>">
                <%= i %>
            </a>
            <% }} %>
                …
                <a href="/500">500</a>
    </footer>
</body>

</html>